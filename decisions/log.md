# Decision Log

Append-only. When a meaningful decision is made, log it here.

Format: [YYYY-MM-DD] DECISION: ... | REASONING: ... | CONTEXT: ...

---

[2026-08-13] DECISION: Track emails awaiting Bryce's reply in context/pending-responses.md, auto-loaded via CLAUDE.md. | REASONING: Bryce wants pending responses surfaced automatically whenever he asks for today's plan or task list, without re-asking each time. | CONTEXT: First populated from a live Gmail scan on 2026-08-13. It's a snapshot, not a live feed — needs re-scanning periodically to stay accurate (candidate for a proper skill later, per the "Lead follow-up system" backlog item in CLAUDE.md).

[2026-08-14] DECISION: Built the `lead-follow-up` skill on top of `references/sops/follow-up-masterclass.md` (Evan Brown's B2B follow-up framework), and wired a lightweight timing flag into `daily-plan` (non-close calls today need the Day-1 sequence run; end-of-month squeeze window flagged in the last 7 days of the month). | REASONING: Bryce asked to turn the masterclass doc into a working system and get it surfaced on the daily task view. Chose draft-and-review over live automation when asked directly: Claude drafts the recap email (Gmail draft), personal text copy, and voice note script, and stages a Close.io task with that copy, but Bryce sends everything himself. Texts are never automated per the SOP's own rule (must come from his personal phone) and building a live Close.io workflow would auto-email real leads without review, which is a bigger step than he asked for. | CONTEXT: Also confirmed the SOP's "Custom Program Overview video" doesn't exist yet for Icarus, recap email template uses a placeholder until it's built. Revisit the live-automation question later if Bryce wants Day 4+ emails to actually auto-send from Close.

[2026-08-14] DECISION: Switched call-happened detection from "trust the calendar" to "confirm via Gemini notes / read.ai transcript email." `daily-plan` no longer excludes `gemini-notes@google.com` from its Gmail scan, and now classifies transcript emails as a "Call confirmed" bucket that upgrades a calendar event from scheduled to confirmed. `lead-follow-up` won't draft a Day-1 sequence for a scheduled call until it's confirmed this way (or Bryce says directly that it happened). A scheduled call with no transcript by the next check is flagged as a likely no-show rather than silently assumed either way. | REASONING: Bryce asked how the assistant knows about his calls and flagged that the calendar alone isn't reliable, it shows what's scheduled, not what happened (no-shows, reschedules, off-calendar calls). Offered three options (transcript-based, calendar-only, tell-me-directly), Bryce picked transcript-based. | CONTEXT: Icarus already auto-generates Gemini notes or read.ai summaries for every call per `context/work.md`, so this reuses an existing signal rather than adding new infrastructure. This directly enabled the no-show workflow decision below.

[2026-08-14] DECISION: Built a Pipeline Management SOP (published artifact, grounded in `references/sops/pipeline-management-masterclass.md`) and the `pipeline-review` skill, after auditing the real Close.io setup first. Findings that shaped the design: one pipeline ("Sales": New Lead, Qualified, Discovery Completed, Proposal Sent, Won, Lost, with Proposal Sent added the same day), a pre-existing "Show Rate" lead custom field (No Show/Showed) that's separate from the No Show lead status the earlier workflow triggers on, zero opportunity custom fields, zero lead smart views, and 368 active opportunities that look like a bulk import: all sampled records stuck in New Lead, $0 value, flat 50% confidence, real details buried in free-text notes. | REASONING: Bryce asked for the SOP and to "build out anything on Close.io that is needed" off the back of it. Discovered the available Close MCP tools can create pipelines/statuses/workflows but have no create or update action for custom fields or smart views, so redesigned around that constraint instead of pretending otherwise: daily 1-10 deal scoring maps onto Close's existing `confidence` field (score x10) rather than a new field, and the stale-deal sweep uses `find_opportunities`' built-in `needs_attention`/`updated_at` filters instead of a smart view. A genuinely new "Lead Source" field got flagged as something Bryce needs to add himself in the Close UI, not something faked. | CONTEXT: Did not touch the 368-opportunity backlog, that's real deal data and a bulk edit needs Bryce's review, not a silent fix, flagged as the SOP's first action item and the skill offers to help triage it in batches on request.

[2026-08-14] DECISION: Rebuilt the no-show workflow's trigger from `lead-manual` (separate enroll step) to `lead-event` filtered to the Close "No Show" lead status, so updating a lead's status to No Show in Close is now the enrollment action itself, no second manual step. New workflow: `Icarus No-Show Re-Engagement (Auto)` (`seq_5SUflaFnSekcrpnNBYyyvK`), same 3 templates and timing as the original. The original `lead-manual` version (`seq_2ZmnyaXkv9JIGYQwe8Clis`) is superseded and orphaned, Close's workflow tools have no update or delete action, so it can't be edited in place or removed via API, Bryce needs to delete it manually in the Close UI. | REASONING: Bryce explicitly asked for the status-change-triggers-it version instead, since he was already going to update the lead status to keep Close accurate, this collapses two actions into one. Still requires the workflow to be switched to Active before anything sends, that gate is unchanged. | CONTEXT: This does remove a safety gate that existed before, previously "mark as No Show" and "start re-engagement" were two separate deliberate actions, now they're one. Documented in the `lead-follow-up` skill that setting a lead's status to No Show must only happen with Bryce's explicit go-ahead, same bar as directly enrolling in a workflow, not treated as a lesser/cosmetic status update anymore.

[2026-08-14] DECISION: Ran a bulk data-quality fix on the 368-opportunity import backlog identified in the pipeline audit, targeted specifically at Show Rate / No Show accuracy (not the full value/confidence hygiene fix, that's still open). Confirmed the mapping with Bryce first: `Original Status` "Closing Call" or "Show - Need Close Call" → Show Rate "Showed"; "No Show" → Show Rate "No Show" AND lead status set to "No Show"; "DQ"/"Opt Out"/"Needs Reschedule"/blank/Calendly-only leads → skipped, not guessed at. Result: 22 leads verified already-correct as Showed, 16 leads had their lead status corrected to No Show (Show Rate was already right, status wasn't), 125 leads confirmed as having no outcome data at all. 13 leads flagged ambiguous (9 Needs Reschedule, 3 DQ, 1 Opt Out) and 1 lead with an unrecognized status value ("Rescheduled", distinct from "Needs Reschedule") were left untouched pending Bryce's review. No conflicts, no errors. | REASONING: Bryce asked to fix show/no-show data using the original spreadsheet import as the source of truth. Investigated the actual note format first rather than assuming a clean 1:1 mapping existed, found two import formats and a non-obvious status vocabulary, then got Bryce's explicit sign-off on the ambiguous cases before writing anything, since this data now feeds the No Show automation trigger and bad data there means wrong leads get enrolled once that workflow goes active. | CONTEXT: This only fixes Show Rate/status accuracy. The broader hygiene issue from the pipeline audit (opportunities stuck in New Lead, $0 value, flat 50% confidence) is still open and untouched.

[2026-08-14] DECISION: Built a second Close.io workflow, `Icarus No-Show Re-Engagement` (`seq_2ZmnyaXkv9JIGYQwe8Clis`), for leads who booked a discovery call and never showed, distinct from the existing Day 4+ sequence which is for leads who took the call and went quiet. 3 email steps at ~1hr/day3/day7 after enrollment, each linking Bryce's real Calendly scheduling link (`https://calendly.com/d/cvjn-rjx-whr/strategy-session-with-icarus-30min`, pulled live from Close via `find_scheduling_links`). Same safety pattern as the first workflow: Draft status, `lead-manual` trigger, requires Bryce to activate it and enroll each lead by hand. Also wired the Day 3 → Day 4+ handoff and the no-show bucket into both `lead-follow-up`'s pipeline check and `daily-plan`'s output, so both surface automatically whenever Bryce asks for his plan, not just on request. | REASONING: Bryce explicitly asked for this once he saw how no-shows would get detected (transcript-absence signal above), and asked that the Day 3 to Day 4+ handoff specifically show up in daily tasks rather than requiring him to remember to ask about it. | CONTEXT: Confirmed via `find_lead_statuses` that Close already has a "No Show" lead status (`stat_yTkv6sYexeEqRhUeJ9GojDr5Ro1rfW1M2z9FzBqOAlG`) to pair with this workflow. No lead status or workflow gets touched automatically, the no-show flag always requires Bryce's confirmation first since "no transcript yet" isn't proof, just a signal.

[2026-08-14] DECISION: Added Day 2-3 touchpoint tracking to the `lead-follow-up` skill (new bucket in the Step 5 pipeline check, surfaced in `daily-plan` every run, not just on request) and built two reference artifacts: a "Strategy Call Script" (the call flow plus the end-of-call phone-number/show-rate tie-downs) and the earlier "Follow-Up Playbook". | REASONING: Bryce flagged that with his call volume he needs help not forgetting the Days 1-3 manual touchpoint window. The SOP only scripts Day 1's content in full and Day 4+'s handoff to Close, it never specifies Day 2 or Day 3, so filled that gap with a light personal-text touch each day, distinct angle each time, no repeats. Made the check run by default every time `lead-follow-up` or `daily-plan` executes, not only when asked, since this is precisely the window most likely to silently drop on a busy day. | CONTEXT: This is an interpretive addition, not sourced from the original masterclass doc, flagged as such in the SOP file itself so it's not mistaken for Evan Brown's original framework.

[2026-08-14] DECISION: Actually built the Day 4+ automated sequence as a real Close.io workflow when Bryce explicitly asked for it (`Icarus Follow-Up: Day 4+ Sequence`, seq_5ihhG5wGdldYXDVSjbbEHR), 4 email touches at ~day 4/8/14/21 with distinct angles (conviction, proof/stats, objection pre-empt, close-the-loop), each in its own new email template (tmpl_UqhQS8..., tmpl_ecydyx..., tmpl_oWYzn1..., tmpl_O5kan4...). | REASONING: Close.io workflows are created in Draft status by default and don't send anything until manually activated, and this one uses a `lead-manual` trigger (enroll one lead at a time), so building it doesn't cross the "don't auto-email real leads" line from the earlier decision, it's inert until Bryce takes two separate deliberate actions: switch it to Active, then enroll a specific lead. No lead status existed in Close that cleanly mapped to "ready for Day 4+ nurture" (statuses are Potential/Qualified/Interested/discovery scheduled/No Show/Bad Fit/Not Interested/Disqualified/Canceled/Customer), so went with manual enrollment rather than creating a new shared status or an automatic status-based trigger. | CONTEXT: Workflow and templates are Bryce's org account only (org has 1 active user, Bryce). He still needs to review the email copy and switch the workflow to Active in Close before it does anything. No existing email templates or workflows were in Close prior to this build, so no collision risk.

[2026-08-14] DECISION: Built the `booking-research` skill (new Google Calendar booking → Perplexity research → Slack DM to Bryce) plus a `context/booking-research-log.md` state file to dedupe repeated runs, and set it up as a recurring scheduled cron job rather than a real-time webhook trigger. | REASONING: Bryce asked to research a client via Perplexity when they book a call and text him the details. Neither Perplexity nor Slack MCP was connected at the time (only Gmail/Calendar/Drive/Close.io were), and there's no SMS integration in this workspace at all. Asked Bryce directly on all three open questions: he'll connect Perplexity MCP himself, delivery goes to Slack DM (not real SMS, not email) since Slack is already the team's tool, and the trigger is a scheduled poll of the calendar (true booking-event webhooks aren't available here) rather than folding it into daily-plan or making it fully manual. | CONTEXT: The skill is written and wired into CLAUDE.md's skill list but functionally blocked until Bryce authorizes both Perplexity MCP and Slack MCP; it degrades gracefully (logs bookings as "detected, pending research" instead of failing) if run before either is connected. Bookings are matched against the Calendly-sourced "Strategy Session with Icarus" title convention from `context/work.md`. Revisit the polling interval and the exact Slack delivery target (self-DM vs a channel) once both connectors are live and the first real run happens.

[2026-08-14] DECISION: Switched `booking-research`'s delivery channel from a Slack DM to email (Gmail, to Bryce's own inbox), and moved the whole skill onto a claude.ai cloud routine (hourly cron, since cloud routines have a 1-hour minimum interval, not the originally planned 30 min) instead of anything running purely in-session. | REASONING: When actually connecting Slack, Bryce clarified Icarus's Slack workspace is company-owned and he isn't the admin, so authorizing a new connector app there needs someone else's approval, not just his login. Given the choice between waiting on that approval or using Gmail (already connected, fully his), he picked email. Separately, discovered mid-build that a cloud routine needs (a) a Git remote to see the skill/context files at all, since cloud sessions get an isolated checkout, and (b) claude.ai's GitHub connector authorized before it'll accept a repo source, neither of which existed yet. | CONTEXT: Bryce chose to push this workspace to a new private GitHub repo (`github.com/bryce-icarus/ea-icarus`) rather than abandon the cloud-routine approach, accepting that real client/lead/revenue data now lives in that private repo. Routine creation is still blocked pending Bryce authorizing the GitHub connector at claude.ai/customize/connectors (separate from creating the repo itself) — once that's done the hourly routine (Google Calendar + Close.io connectors attached) gets created as configured. Perplexity is still the other open dependency, unchanged from the prior entry.

[2026-08-15] DECISION: Created the `booking-research` cloud routine (`trig_01TMhbXNeURS5xUVqZLRM3CE`) once GitHub access cleared, then narrowed its schedule from hourly/24-7 to hourly during business hours only (7am-3pm Central, Monday-Friday, `0 12-20 * * 1-5` UTC), renamed to `booking-research-business-hours`. | REASONING: Getting GitHub connected required two steps, not one: authorizing the connector at claude.ai/customize/connectors, then separately granting the resulting GitHub App access to the specific `ea-icarus` repo (it defaults to no/selected-repo access), the first attempt after "connecting" GitHub still 403'd until Bryce did the second step. On the schedule, Bryce asked whether polling hourly would burn more credits, correctly since every run costs something (session spin-up, repo clone, calendar check) even when it finds no new booking, and bookings/calls don't happen at 3am anyway, so business-hours-only cuts run count from 24/day to 9/day with no real loss of responsiveness. | CONTEXT: Routine is live and will fire correctly even before Perplexity is connected (logs bookings as pending research per the skill's degrade-gracefully step). Gmail, Google Calendar, and Close connectors are attached; Perplexity still needs to be added to the routine's `mcp_connections` once Bryce authorizes it.

[2026-08-15] DECISION: Ran a manual demo of `booking-research` against a real booking (Mohamed / ArkonLabs Research, discovery call Mon Aug 17), using WebSearch as a stand-in for the still-unconnected Perplexity, and emailed the resulting brief to Bryce. Logged the booking as processed in `context/booking-research-log.md` so the scheduled routine won't duplicate-send it. Bryce then deprioritized connecting Perplexity for now (its remote MCP server authenticates via API key, and claude.ai's web custom-connector flow currently requires OAuth, so it can't be added there at all, only through Claude Code locally, e.g. `claude mcp add --transport http`, which doesn't help the cloud routine). | REASONING: Bryce wanted to see the skill's actual output before deciding whether to chase the Perplexity connection further. Demo used the lead's own Calendly submission (explicitly asking for restricted-vertical/compliance experience) as the strongest available signal, since that's stronger than what generic web search turned up on a thin, newer company. | CONTEXT: `booking-research`'s Prerequisites section already documents the graceful-degradation behavior (log as pending, backfill later) for exactly this situation, so leaving Perplexity disconnected doesn't break anything, it just means research stays at WebSearch-depth (found the company site, a product description, no funding/press/ad-history signals) rather than going deeper. Revisit if Bryce wants that depth later.

[2026-08-15] DECISION: Added a Step 5 to `booking-research`: draft a short personal pre-call text in Bryce's own voice for every new booking, staged in the email brief (never sent automatically, same rule as every other text in this workspace). | REASONING: Bryce asked directly whether any SOP covers reaching out right after a lead books, in his own language. Checked all the SOP files first rather than assuming, confirmed the real gap: `confirmation-page-best-practices.md` covers the automated Calendly confirmation page and reminder cadence (marketing/Simon's territory, not a personal Bryce touch), and every other SOP either covers post-call follow-up or pre-booking outbound, neither applies to Icarus's actual flow (leads self-book via Calendly, no setter). Reused the voice rules already established and approved elsewhere in this workspace (`follow-up-masterclass.md` Sections 1 and 11: conviction-based, direct, personal, never "just checking in") rather than inventing a new tone, and required the draft to reference something specific from the booking notes or research every time, refusing to draft a hollow generic line if nothing specific surfaced. | CONTEXT: Explicitly flagged in the skill file as an interpretive addition, not sourced from the original masterclass material, same transparency pattern used for the Day 2-3 touch gap fill. Worked example added to the skill using the real ArkonLabs/Mohamed demo booking.

[2026-08-15] DECISION: Briefly tried delivering the pre-call text as its own standalone Gmail draft (separate from the research-brief email) at Bryce's request, then reverted to the single combined email at his follow-up request. | REASONING: Bryce wanted to see both formats before deciding. Preferred everything in one email. | CONTEXT: No functional difference otherwise, both used `mcp__claude_ai_Gmail__create_draft` / `send_message` on the same connector.

[2026-08-15] DECISION: Simplified `booking-research` significantly: dropped Perplexity entirely (Bryce deprioritized chasing the connection), switched the research step to `WebSearch` (already available, no setup, added to the cloud routine's `allowed_tools`), and cut the full "Research" brief section out of the output altogether, the skill now only produces one thing, a tailored personal pre-call text. | REASONING: Two corrections from Bryce during a full pipeline walkthrough. First, the "confirmation page" the skill's own docs referenced as existing marketing-owned automation (per `confirmation-page-best-practices.md`) doesn't actually exist yet at Icarus, that's still a to-build item on Bryce's side, flagged and corrected in `context/current-priorities.md` rather than left stated as live. Second, Bryce pointed out the research brief itself was redundant: ClickUp's Discovery Call Prep Assistant already auto-generates a pre-call brief per `context/work.md`, so `booking-research` re-doing that was duplicate work. He explicitly said research can be "very short," skimmable, and that the booking notes/Calendly submission are often good enough on their own (borne out by the ArkonLabs demo, where the lead's own words beat what WebSearch found). | CONTEXT: The skill's sole output is now the drafted text, sourced first from the booking's own Calendly notes and Close history, with WebSearch as a fallback only when those aren't enough. No missing dependencies remain, fully functional as of this change. `booking-research-business-hours` routine updated in place (same trigger ID) with `WebSearch` added to allowed tools.

[2026-08-15] DECISION: Ran a full manual Day-1 follow-up sequence for a real call (Caleb Dornay, Nic Nac, confirmed via Gemini notes) at Bryce's request, to demo the flow: recap email drafted in Gmail (referencing the Stage 1 proposal he'd already sent same day, rather than duplicating it), personal text and voice note script staged on a high-priority Close task, confirmation note logged on the lead. | REASONING: Bryce wanted to see a real example against a real call, same demo pattern used for `booking-research`. Surfaced two data-quality issues in passing rather than fixing them silently: duplicate Nic Nac lead records in Close, and a status still reading "discovery scheduled" despite the call and proposal already happening. | CONTEXT: This is exactly the `lead-follow-up` skill's Step 2-4 behavior, executed manually. Directly led to the next decision below once Bryce saw it and asked to automate the trigger.

[2026-08-15] DECISION: Built `day1-followup-auto`, a new cloud routine that watches Gmail for new call transcripts and automatically runs the Day-1 follow-up sequence (same output as the Nic Nac manual run: recap Gmail draft, text/voice note staged on a Close task, confirmation note), on the same hourly business-hours schedule as `booking-research-business-hours`. Added a dedup check to `lead-follow-up` itself (look for the "Day-1 follow-up sequence generated" note before drafting) so manual runs and the new routine can't double up on the same call. | REASONING: Bryce asked for this to run automatically the moment a Gemini transcript appears, same shape as `booking-research`. He initially asked for a 30-minute interval; same 1-hour cloud-routine floor from the `booking-research` build applies here too, so it's on the identical hourly/business-hours cron instead ("keep the same times as the other" — satisfied literally, same schedule, same routine pattern). No new MCP connectors needed, reuses the same three (Gmail, Google Calendar, Close) already on the booking-research routine. | CONTEXT: This is now the second scheduled cloud routine in this workspace; both run on `0 12-20 * * 1-5` UTC (7am-3pm Central, Mon-Fri). Neither sends anything itself, both stop at drafting/staging, preserving the "texts and real sends stay manual" rule that runs through every skill in this workspace.
